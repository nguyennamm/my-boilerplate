/*
 * MenuColumn Messages
 *
 * This contains all the text for the MenuColumn component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MenuColumn';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MenuColumn component!',
  },
});
