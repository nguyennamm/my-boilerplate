/*
 * HeaderBox Messages
 *
 * This contains all the text for the HeaderBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderBox component!',
  },
});
